<?xml version="1.0" encoding="utf-8"?>
<!-- Place at res/xml/accessibility_service_config.xml -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRetrieveInteractiveWindows|flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:description="@string/accessibility_service_description" />

<!--
  No android:packageNames attribute on purpose — the blocklist is
  user-configurable at runtime (stored in Room's UserSettings table),
  so the service needs events from every app and filters in Kotlin
  against that table. If you hardcode packageNames here instead, you
  can't let users add/remove monitored apps without a rebuild.
-->
