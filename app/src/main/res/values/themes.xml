<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.BloomScrolling" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Tema da SurveyActivity: a janela fica transparente, então o app que
         está atrás continua visível. O escurecimento é feito no Compose. -->
    <style name="Theme.BloomScrolling.Translucent" parent="Theme.BloomScrolling">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
